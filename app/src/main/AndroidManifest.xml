<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".user_customization"
            android:exported="false" />
        <!-- Define the main activity that will be launched when the application starts -->
        <activity
            android:name=".login_page"
            android:exported="true">
            <intent-filter>

                <!-- Declare the main action for the intent filter -->
                <action android:name="android.intent.action.MAIN" />
                <!-- Declare the main category for the intent filter -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Define other activities -->
        <activity
            android:name=".addField"
            android:exported="false" />
        <activity
            android:name=".add_event"
            android:exported="false" />
        <activity
            android:name=".add_business"
            android:exported="false" />
        <activity
            android:name=".add_friends_page"
            android:exported="false" />
        <activity
            android:name=".booking_page"
            android:exported="false" />
        <activity
            android:name=".fields_events_details_page"
            android:exported="false" />
        <activity
            android:name=".business_field_details_page"
            android:exported="false" />
        <activity
            android:name=".friends_chats_page"
            android:exported="false" />
        <activity
            android:name=".event_page"
            android:exported="false" />
        <activity
            android:name=".friends_page"
            android:exported="false" />
        <activity
            android:name=".registration_page"
            android:exported="false" />
        <activity
            android:name=".home_page"
            android:exported="false" />

        <activity
            android:name=".calender_page"
            android:exported="false" />

    </application>

</manifest>